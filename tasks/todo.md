# Current task

> Working scratchpad for the task in flight. Write the plan here as checkable items,
> mark them off as you go, and add a short Review section when the task closes.
>
> **This file is not an archive.** Once a task ships, its plan is history — the durable
> outcome belongs in `CLAUDE.md` (facts/rules), `tasks/lessons/inbox/` (a lesson), or the
> commit message. Clear this file at the start of the next task instead of appending to it.
> Stale plans left here read as an open backlog and cost a future session a wrong turn:
> a 517-line graveyard with 23 unchecked boxes on already-shipped work was cleared
> 2026-08-17 for exactly that reason.

_(nothing in flight)_
